---
description: 
globs: 
alwaysApply: true
---
# 项目标准和工作流程

本规则定义了AstroPaper博客项目的开发标准、工作流程和质量要求。

## 核心开发原则

### 1. 知识库优先原则
- 始终参考 `.cursor/rules/` 目录作为项目知识库
- 在编写任何代码前，先检查相关规则文件
- 不要在项目上下文之外编写代码
- 如有疑问，优先查阅规则文件而非猜测

### 2. 实施计划遵循
- 开发新功能时，严格遵循 `implementation-plan.mdc` 中的步骤
- 按顺序完成每个步骤，不跳过任何环节
- 完成每个步骤后，在计划文件中标记"Done"并添加两行总结
- 保持清晰的工作日志，确保透明度和进度跟踪

### 3. 渐进式开发方式
- 逐个文件进行修改，给用户检查错误的机会
- 避免一次性大规模修改
- 在单个文件编辑中提供所有修改，不分多步骤说明

## 代码质量标准

### 1. 命名规范
- 使用描述性、明确的变量名，避免简短、模糊的名称
- 优先选择完整的英文单词而非缩写
- 函数名应清楚表达其功能和用途
- 常量使用大写字母和下划线分隔

### 2. 代码结构
- 遵循项目现有的编码风格保持一致性
- 实现模块化架构提高代码可维护性和可复用性
- 避免魔法数字，使用有意义的常量替代
- 实现健壮的错误处理机制

### 3. 性能和安全
- 编写代码时优先考虑性能优化
- 采用安全优先的方法，考虑所有潜在安全风险
- 处理逻辑时考虑并处理潜在的边界情况
- 在适当位置使用断言验证假设并及早发现错误

## 文件和代码管理

### 1. 文件操作原则
- 优先编辑现有文件而非创建新文件
- 除非绝对必要，否则不创建新文件
- 永远不要主动创建文档文件（*.md）或README文件，除非用户明确要求
- 保持现有代码结构，不删除无关的代码或功能

### 2. 版本兼容性
- 确保所有修改与项目指定的语言或框架版本兼容
- 如出现版本冲突，提供替代方案或向后兼容的解决方案
- 检查 [package.json](mdc:package.json) 中的依赖版本要求

### 3. 测试和验证
- 为新增或修改的代码提供适当的测试
- 不要要求用户手动验证在上下文中可见的实现
- 如修改影响功能，提供自动化检查或测试而非手动验证请求

## 交流和文档规范

### 1. 交流风格
- 使用简体中文与用户交流
- 不使用道歉用语
- 避免在评论或文档中提供理解反馈
- 不提供不必要的更改摘要，除非用户明确要求

### 2. 代码注释
- 使用中文编写代码注释
- 注释应解释"为什么"而不仅仅是"做什么"
- 对复杂逻辑提供清晰的解释
- 避免显而易见的注释

### 3. 提交和更改
- 不建议空白字符更改
- 不编造用户未明确要求的更改
- 不在没有实际修改的情况下建议更新文件
- 始终提供真实文件链接，而非上下文生成的文件链接

## 项目特定规范

### 1. AstroPaper博客结构
- 博客内容位于 [src/data/blog/](mdc:src/data/blog) 目录
- 遵循 [.cursor/rules/writing-md-rule.mdc](mdc:.cursor/rules/writing-md-rule.mdc) 的写作规范
- 使用中文直角引号「」而非英文引号""（除非引用内容全为英文）

### 2. 组件开发
- Astro组件使用 `.astro` 扩展名
- React/Vue/Svelte组件仅在必要时使用
- 利用Astro的部分水合和多框架支持
- 优先使用静态生成和最少的JavaScript

### 3. 样式和设计
- 使用Tailwind CSS工具类
- 永远不使用@apply指令
- 实现响应式设计使用CSS自定义属性和媒体查询
- 保持设计系统的一致性

## 错误处理和调试

### 1. 错误修复流程
- 如引入代码检查错误，在清楚如何修复时立即修复
- 不要在同一文件上超过3次修复代码检查错误的循环
- 第三次时停止并询问用户下一步操作
- 不要进行无根据的猜测

### 2. 性能监控
- 优先考虑Core Web Vitals（LCP、FID、CLS）
- 使用Lighthouse和WebPageTest进行性能审计
- 实施性能预算和监控机制

### 3. 调试工具
- 使用Astro开发工具进行调试
- 利用浏览器开发者工具进行性能分析
- 实施适当的日志记录和错误跟踪

## 质量保证检查清单

### 代码提交前检查
- [ ] 代码遵循项目现有风格
- [ ] 所有变量使用描述性名称
- [ ] 实现了适当的错误处理
- [ ] 考虑了边界情况
- [ ] 添加了必要的类型定义
- [ ] 性能和安全性得到考虑
- [ ] 测试覆盖充分

### 功能完整性检查
- [ ] 功能按预期工作
- [ ] 没有破坏现有功能
- [ ] 符合用户需求和规格
- [ ] 文档更新（如需要）
- [ ] 版本兼容性确认
