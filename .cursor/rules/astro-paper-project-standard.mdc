---
alwaysApply: true
---
## AstroPaper 博客项目规范（全局规则）

请将本规则作为项目内一切开发活动的基础约束。关键入口文件：

- 站点配置：[src/config.ts](mdc:src/config.ts)
- 内容集合与 Frontmatter 模式：[src/content.config.ts](mdc:src/content.config.ts)
- Astro 配置与 Markdown 管线：[astro.config.ts](mdc:astro.config.ts)
- ESLint 配置：[eslint.config.js](mdc:eslint.config.js)
- 文章目录：`src/data/blog/`
- UI 组件：`src/components/`、布局：`src/layouts/`、页面：`src/pages/`

### 语言与沟通
- 所有对外交流与文档使用简体中文。
- 中文文本统一使用直角引号「」，仅在全英文引用中使用英文引号。

### 代码与质量
- 统一使用 TypeScript，类型显式、变量命名清晰，避免缩写与含糊命名。
- 遵循 ESLint 约束（包含 `no-console`）。必要日志请采用更合适的方式或在允许范围内使用。
- 处理边界与错误：对 I/O、时间、网络、解析等做健壮的错误处理。
- 避免魔法数字，使用具名常量。

### 性能与安全
- 优先静态生成与最小化客户端 JavaScript。
- 样式使用 Tailwind CSS 工具类，避免 `@apply`。
- 输入校验与输出转义，使用 HTTPS 与安全响应头，依赖定期更新。

### 内容与路由
- 博客内容位于 `src/data/blog/`，支持多级子目录；路径由 [src/utils/getPath.ts](mdc:src/utils/getPath.ts) 计算。
- 文章筛选与排序遵循：
  - 发布过滤：[src/utils/postFilter.ts](mdc:src/utils/postFilter.ts)（未来时间文章在生产环境中会延迟至 `SITE.scheduledPostMargin` 之后展示）。
  - 排序逻辑：[src/utils/getSortedPosts.ts](mdc:src/utils/getSortedPosts.ts)。
  - 标签归并与去重：[src/utils/getUniqueTags.ts](mdc:src/utils/getUniqueTags.ts)，标签 slug 规则见 [src/utils/slugify.ts](mdc:src/utils/slugify.ts)。

### Markdown 渲染管线
- 目录与可折叠：`remark-toc` 与 `remark-collapse` 已启用（关键字 “Table of contents”）。详见 [astro.config.ts](mdc:astro.config.ts)。
- Mermaid：推荐在 Markdown 中使用 ` ```mermaid` 代码块，由前端客户端组件渲染（参见 `src/components/MermaidClient.astro` / `MermaidDiagram.astro`）。

### OG/RSS/站点输出
- 动态 OG：站点/文章 OG 由 Satori + Resvg 生成，参见：
  - 文章 OG 模板：[src/utils/og-templates/post.js](mdc:src/utils/og-templates/post.js)
  - 生成方法：[src/utils/generateOgImages.ts](mdc:src/utils/generateOgImages.ts)
  - 文章路由输出：[src/pages/posts/[...slug]/index.png.ts](mdc:src/pages/posts/[...slug]/index.png.ts)
  - 站点 OG：[src/pages/og.png.ts](mdc:src/pages/og.png.ts)
- RSS 限制：仅输出最新 10 篇，见 [src/pages/rss.xml.ts](mdc:src/pages/rss.xml.ts)。

### 样式与组件
- Astro 组件复用优先；仅在必要时引入前端框架。使用 `Astro.props` 与类型定义。
- 全站 Tailwind，保持响应式与一致性，不使用 `@apply`。

### 开发工作流
- 命令：
  - 开发：`pnpm dev`
  - 构建：`pnpm build`
  - 预览：`pnpm preview`
  - 检查：`pnpm lint`
  - 格式化：`pnpm format`
  - 校验描述（SEO）：`pnpm validate:meta`（脚本见 [scripts/validate-meta-descriptions.ts](mdc:scripts/validate-meta-descriptions.ts)）

### 实施计划
- 新功能严格遵循 `implementation-plan.mdc`，逐步完成并在每步标记「Done」并附两行总结。
- 渐进式提交：单文件集中编辑，保留现有结构，不做无关更改。

### 可访问性与 SEO
- 语义化 HTML、合理 ARIA、键盘可达。
- 关注 Core Web Vitals；图片优化与懒加载。
- Meta Description 建议 150–160 字符（脚本容忍 120–160）。

### 故障排除
- 优先确认 Node/pnpm 版本、Astro 配置、依赖安装与构建日志。

