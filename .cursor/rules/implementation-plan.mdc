---
alwaysApply: false
description: 功能开发实施计划模板与执行要求
---
## 功能开发实施计划模板

按以下步骤推进，每步完成后在标题后追加「Done」并写两行总结：

### 1. 背景与目标
- 明确业务背景与成功标准。
- 列出受影响模块与边界条件。

### 2. 设计方案与取舍
- 说明技术路线、数据结构、路由与交互。
- 列出替代方案与取舍理由（性能/安全/可维护性）。

### 3. 任务分解与文件清单
- 精确到文件与函数：组件、工具、页面、样式、脚本。
- 标注是否新建或修改，避免无关改动。

### 4. 风险与边界用例
- 列出错误场景、空数据、时区/时间、网络失败等。
- 提出回退策略与监控点。

### 5. 实施（按文件逐个提交）
- 每个文件在一次编辑中完成全部修改，保持一致风格。
- 添加类型、断言与必要的错误处理。

### 6. 自测与脚本
- 运行：`pnpm lint`、`pnpm format`、`pnpm build`、必要时 `pnpm preview`。
- 针对变更添加自动检查或脚本（如 SEO、链接检查等）。

### 7. 文档与可观测性
- 更新相关注释与规则文件（如有影响）。
- 记录关键指标与监控方案。

### 8. 完成标准与验收
- 通过所有检查构建；不破坏现有功能与结构。
- 版本兼容、性能预算与安全审计达标。

