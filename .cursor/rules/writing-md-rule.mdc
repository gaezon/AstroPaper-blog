---
globs: src/data/blog/**/*.md,src/pages/**/*.md
description: 博客与静态页面 Markdown 写作与 Frontmatter 规范
---
## Markdown 写作规范与 Frontmatter 校验

### Frontmatter 必填/可选字段（参照 [src/content.config.ts](mdc:src/content.config.ts)）
- 必填：
  - `pubDatetime: ISO8601 日期时间`
  - `title: 字符串`
  - `description: 字符串（建议 150–160 字符，脚本允许 120–160）`
- 可选：
  - `author: 字符串（默认 [src/config.ts](mdc:src/config.ts) 的 `SITE.author`）`
  - `modDatetime: ISO8601 日期时间`
  - `featured: boolean`
  - `draft: boolean`
  - `tags: string[]（默认 ["others"]）`
  - `ogImage: 图片路径或 Astro image()`
  - `canonicalURL: 字符串`
  - `hideEditPost: boolean`
  - `timezone: 字符串（IANA 格式）`
  - `slug: 字符串（可选；当前路由由文件路径决定，不改变路由）`

示例片段：

```yaml
---
author: gaazeon
pubDatetime: 2025-01-01T10:00:00.000+08:00
modDatetime: 2025-01-02T08:00:00.000+08:00
title: 文章标题
featured: false
draft: false
tags:
  - Web
  - Astro
description: 一段 150–160 字符的摘要，覆盖文章核心信息、关键词与读者收益点，避免堆砌与口水话。
slug: optional-custom-slug-for-display-or-scripts
---
```

### 文章结构要求
- 必含段落：前言、正文、总结、参考资料（若需要）。
- 清晰的标题层级，从 `##` 起步，避免跳级。
- 表达简洁、段落短句；中文全角标点与直角引号「」。
- 代码块提供可运行或能复制的片段，指明语言高亮。
- 必要时使用 Mermaid 图（在 Markdown 中用 ` ```mermaid`）。

### 链接与资源
- 外链使用 Markdown 链接，必要时补充简短注记。
- 图片应提供清晰文件名与 Alt 文本；体积优化，必要时使用 `public/` 静态资源。

### 标签与路径
- 标签会通过 [getUniqueTags.ts](mdc:src/utils/getUniqueTags.ts) 统一去重与排序，slug 化规则见 [slugify.ts](mdc:src/utils/slugify.ts)。
- 支持多级子目录；最终路由由 [getPath.ts](mdc:src/utils/getPath.ts) 决定。

### 发布与定时
- `draft: true` 不会被生产环境收录。
- 未来发布时间的文章将在超出 `SITE.scheduledPostMargin` 后自动显示，参见 [postFilter.ts](mdc:src/utils/postFilter.ts)。

### SEO 建议
- `description` 控制在 150–160 字符最佳（校验脚本：`pnpm validate:meta`）。
- 避免关键词堆砌；首段直入主题，可包含 1–2 个核心关键词。
- 如有 Canonical，填写 `canonicalURL`。

### Mermaid 使用
- Markdown 中以 ` ```mermaid` 书写，客户端由 `MermaidClient.astro`/`MermaidDiagram.astro` 渲染。
- 主题随页面明暗模式自动切换；复杂图表请拆分并添加必要注释文字说明。

### 文风细则
- 使用主动语态；避免口水化表达与无效铺陈。
- 把结论、配方、清单优先呈现；图文并茂，使用表格时注意可读性。

