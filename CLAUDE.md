# Claude Code Notes

- Follow [AGENTS.md](AGENTS.md) for repository conventions and verification. Read the sections and source files relevant to the current task.
- Use [README.md](README.md) for setup and commands, [WRITING.md](WRITING.md) for article authoring, and the relevant guide under `docs/` for feature details.
- The required toolchain is Node.js `24.x` and pnpm `>=11 <12`; use pnpm for project commands.
- Blog content is in `src/data/blog/` and English translations are in `src/data/blog/en/`. Keep generated files under `src/utils/generated/` in sync through their generators rather than editing them by hand.
