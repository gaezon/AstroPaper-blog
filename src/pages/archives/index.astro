---
import ArchivesPage from "@/components/ArchivesPage.astro";
import { SITE } from "@/config";
import { getBlogPosts } from "@/utils/i18n-pages";

if (!SITE.showArchives) {
  return Astro.redirect("/404");
}

const posts = await getBlogPosts("zh-CN");
---

<ArchivesPage {posts} />
