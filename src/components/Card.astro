---
import { toTransitionName } from "@/utils/toTransitionName";
import { getPath } from "@/utils/getPath";
import { getArticleViewPaths } from "@/utils/article-view-paths";
import Datetime from "./Datetime.astro";
import ArticleViews from "./ArticleViews.astro";
import { t } from "@/i18n/utils";

export interface Props {
  variant?: "h2" | "h3";
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  data: any;
  id: string;
  filePath?: string;
}

const { variant = "h2", data, id, filePath } = Astro.props;

const { title, description, pubDatetime, modDatetime, timezone } = data;
const postPath = getPath(id, filePath, true, data.slug);
const articleViewPaths = getArticleViewPaths(postPath);

const headerProps = {
  style: { viewTransitionName: toTransitionName(title) },
  class: "text-lg font-medium decoration-dashed hover:underline",
};
---

<li class="my-6">
  <a
    href={postPath}
    class="inline-block text-lg font-medium text-accent decoration-dashed underline-offset-4 focus-visible:no-underline focus-visible:underline-offset-0"
    aria-label={t(Astro, "ui.readArticle", { title })}
  >
    {
      variant === "h2" ? (
        <h2 {...headerProps}>{title}</h2>
      ) : (
        <h3 {...headerProps}>{title}</h3>
      )
    }
  </a>
  <div class="flex items-center gap-2">
    <Datetime {pubDatetime} {modDatetime} {timezone} />
    <ArticleViews paths={articleViewPaths} loading="visible" />
  </div>
  <p>{description}</p>
</li>
